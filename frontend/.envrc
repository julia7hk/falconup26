# Load environment variables from the root of the project .env (self-contained).
# _if_exists so a fresh checkout (where .env is gitignored, hence absent)
# doesn't abort direnv.
dotenv_if_exists ../.env
